function greet(name

    : string): string {
    return `Hello
    
    ${name}`;
}

console.log(greet("World     !"));

// const path = Deno.env.get("DENO_INSTALL");

// console.log("Deno Install Path:", path);